<div class="screen full-page" id="trigramRevealScreen">
	<h3>This week's trigram is...</h3>
	<div id="trigramReveal"></div>
	<h4 id="trigramRevealSubtitle"></h4>
</div>

<style>
	#trigramRevealScreen {
		z-index: var(--z-trigramRevealScreen);
		background-color: hsl(var(--c1-hue), var(--c1-sat), var(--c1-med));
		color: hsl(var(--c1-hue), var(--c1-sat), var(--c1-dark));

		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		text-align: center;
	}

	#trigramRevealScreen.showTemporarily {
		animation: show-temporarily 5s ease;
	}

	h3 {
		font-size: 1.5rem;
		padding-bottom: 1rem;
	}

	h4 {
		font-size: 1.125rem;
		padding-top: 0.75rem;
	}

	#trigramReveal {
		width: 14.75rem;
		padding: 1.5rem 1rem;
		border: 6px solid;
		border-radius: 24px;

		display: flex;
		align-items: center;
		justify-content: space-around;

		background-color: white;

		font-size: 4rem;
		font-weight: bold;
	}

	@keyframes show-temporarily {
		10% {
			opacity: 1;
		}
		80% {
			opacity: 1;
		}
		100% {
			opacity: 0;
		}
	}
</style>
