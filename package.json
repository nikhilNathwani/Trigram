{
	"name": "trigram",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "astro dev",
		"build": "astro build && mkdir -p dist/data dist/tools && cp data/trigram_calendar.json dist/data/ && cp -r data/trigram-word-lists dist/data/ && cp -r tools/label dist/tools/",
		"preview": "astro preview",
		"test": "vitest run",
		"test:watch": "vitest",
		"test:e2e": "playwright test",
		"test:e2e:ui": "playwright test --ui",
		"test:all": "npm run test && npm run test:e2e"
	},
	"devDependencies": {
		"@playwright/test": "^1.55.0",
		"astro": "^7.1.1",
		"http-server": "^14.1.1",
		"jsdom": "^25.0.1",
		"vite": "^7.3.6",
		"vitest": "^3.2.4"
	}
}
